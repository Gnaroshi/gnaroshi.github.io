# Project Media Brief: gnaroshi_vla

## Purpose

Show the verified architecture-neutral VLA experiment workspace without presenting unreviewed experiment results as achievements.

## Available Real Artifacts

Website-owned facts confirm Python/Shell/YAML infrastructure, architecture adapters, separated methods/configuration/environment/results, and explicit run context. The public project link points to `Gnaroshi/gnaroshi_vla`.

This Stage 1 task is restricted to `gnaroshi.github.io`, so the linked project repository was not opened or modified. Actual artifact availability must be verified during the approved screenshot collection step.

## Screenshot Needed

Preferred options, in order:

1. A real terminal/tree view of the public repository showing architecture, methods, configuration, scripts, and results boundaries.
2. A real configuration excerpt with no machine paths or secrets.
3. A real run-context or environment-snapshot artifact that makes no benchmark claim.

If none is legible and truthful, the project remains typography-only. Do not generate a replacement diagram or fake terminal.

## Capture Plan

- 16:10 crop, Retina source at least 2400x1500, responsive export at 1200x750.
- Keep code and path text large enough to read at the detail-page width.
- Use a native terminal/editor/app crop without device framing.

## Redaction And Privacy

- Remove local usernames, absolute paths, hostnames, dataset mounts, private repository names, tokens, and node-specific infrastructure details.
- Show only public repository files.
- Do not show benchmark numbers, generated charts, or experiment claims unless separately reviewed and already public.
- Preserve third-party attribution where it is visible and required.

## Alt Text

English: `Public gnaroshi_vla workspace structure separating model adapters, research methods, configuration, and run context.`

Korean: `모델 어댑터와 연구 방법, 설정, 실행 맥락을 분리한 공개 gnaroshi_vla 작업 공간 구조.`

## Final Route Usage

Home Selected Work, `/projects/`, `/projects/gnaroshi-vla/`, and Korean equivalents. A single verified artifact may be reused only across these same-project routes.

Status: artifact inspection and capture deferred to Stage 2 approval.
